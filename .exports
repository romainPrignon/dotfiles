# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

PATH="/usr/local/go/bin:$PATH"
PATH="$HOME/app/PhpStorm/bin:$PATH"
PATH="$HOME/app/android-studio/bin:$PATH"

# Make nano the default editor
export EDITOR="nano";
